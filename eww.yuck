;;(include 'experimental/others.yuck')

(defwindow bars
  :monitor 0
  :geometry (geometry :width '100%' :height '100%')
  :stacking 'bottom'
  :exlusive false
  (box
    :orientation 'horizontal' 
    :class 'barl'
    (label :text 'hi')
  )
)
