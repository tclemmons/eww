(include "widgets/volume.yuck")

(defwindow volume_monitor0 
  :monitor 0
  :geometry
  (geometry
    :width "1%"
    :height "10%"
    :anchor "center center"
  )
  :stacking "fg"
  (volume_widget :initial_volume_level initial_volume_level)
)
